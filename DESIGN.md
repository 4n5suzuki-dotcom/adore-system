# Adore System デザイン方針

恵比寿の会員制ラウンジ **LOUNGE Adore** のブランド世界観を、管理システムの UI に反映するための指針。
（出典：[公式](https://lounge-adore.net/) / [店舗紹介](https://lounge-one.net/introduce/ebisu-adore/) / [店舗情報](https://kyaba-kura.jp/article5/shop/97/)。Instagram @ebisuadore は会員制でフィード非公開のため、公式・紹介情報から世界観を抽出。）

---

## 1. ブランドの世界観

| キーワード | 意味 | UI への落とし込み |
|---|---|---|
| ゴージャス × シック | 華やかさと落ち着きの両立 | 過度な装飾を避け、抑制された上質感 |
| 温かみのシャンデリア／調光 | 暗めで温かい光 | 明るい白基調 → クリーム＋ダーク面の対比 |
| 革張りソファ | レザーの質感 | 深いボルドー、マットな面 |
| 白いソファ × ワインレッドのソファ | 2つの対比エリア | クリーム × ボルドーの配色 |
| VIP＝西洋古城・アンティーク・鏡面 | 欧州調エレガンス | セリフ見出し、ゴールドのヘアライン |

**一言で：大人の・落ち着いた・アンティーク欧州調のエレガンス。きらびやかさより抑制の効いた上質感。**

---

## 2. カラーパレット

軸＝**ボルドー × ゴールド × クリーム × チャコール**（機能色は必要時のみ控えめに）。

| トークン | HEX | 用途 |
|---|---|---|
| `wine-red` | `#6B1F2A` | メイン。見出し・主要ボタン・アクセント |
| `gold` | `#C9A84C` | アクセント。区切り線・強調・サブボタン |
| `brass` | `#A8894C` | アンティークブラス。ヘアライン・控えめな差し色 |
| `cream` | `#FAF6EF` | 温かい背景（白い贅沢ソファのトーン） |
| `charcoal` | `#1A1614` | 調光された暗い面。サイドバー／ヘッダー |
| `white` | `#FFFFFF` | カード面 |
| `gray-50/200/400/600/800` | — | 罫線・補助テキスト |
| `green/orange/red/blue` | （現状維持） | ステータス機能色 |

### 補足
- `wine-red` は旧 `#8B3A3A`（茶色く濁る）から `#6B1F2A` の深いボルドーへ変更。
- **機能色（green/orange/red/blue）は今回未変更**。Material 風で世界観からやや浮くため、将来的にくすませた上品なトーンへ調整する（白文字とのコントラスト確保を要検証）。
  - 提案値（参考）：green `#3E8E78` / orange `#C0833A` / red `#B5454A` / blue `#5A7D9A`

---

## 3. タイポグラフィ

| 役割 | フォント | 指針 |
|---|---|---|
| 見出し | `Cormorant Garamond` → `Shippori Mincho B1`（セリフ/明朝） | 高級感の要。`letter-spacing: 0.02em` でゆとり |
| 本文 | システムサンセリフ | 可読性重視。クリーンに保つ |

- 見出しは `.heading-1` / `.heading-2` を使用（globals.css でセリフ＋字間を定義済み）。
- フォントは現状 Google Fonts の `@import` で読み込み。**本番では `next/font` での読み込みを推奨**（FOUT・パフォーマンス対策）。

---

## 4. コンポーネント原則

- **影は弱く・柔らかく**：`shadow-lg` を多用しない。`.card` は淡い影（`0 2px 14px rgba(26,22,20,.06)`）＋細い罫線。
- **太い border より金のヘアライン**：区切りは `.divider-gold`（1px / brass 50%）。
- **ナビ面はダーク**：サイドバー／ヘッダーは `.surface-dark`（charcoal 背景＋cream 文字）で調光空間を再現。
- **余白を贅沢に**：詰め込まず、ゆったりした padding と行間。
- **写真は暖色・低照度トーン**で統一。

### NG / OK 例
- ❌ 真っ白背景に鮮やかな原色バッジを多用 → 管理ツール感
- ✅ クリーム/白の落ち着いた面 ＋ ボルドー＋金の差し色 ＋ セリフ見出し

---

## 5. 実装メモ（適用済み / 今後）

**適用済み（このコミット）**
- `tailwind.config.js`：`wine-red` 深化、`cream` / `charcoal` / `brass` 追加
- `styles/globals.css`：見出しセリフ化、`.card` の影を軽量化、`.divider-gold` / `.surface-dark` 追加

**今後の展開（段階的に）**
- サイドバー／ヘッダーを `.surface-dark` 化（現状 `bg-wine-red`）
- ページ背景を `bg-white` → `cream` へ統一
- ステータス機能色のトーンダウン
- 重要画面（ダッシュボード等）から順に見出しを `.heading-*` へ統一
